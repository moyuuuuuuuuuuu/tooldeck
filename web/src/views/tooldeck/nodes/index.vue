<template><div><div class="heading"><h1>执行节点</h1><ElButton @click="load">刷新状态</ElButton></div><ElTable :data="nodes"><ElTableColumn prop="name" label="节点名称"/><ElTableColumn label="Docker 连接"><template #default="{row}"><ElTag :type="row.online?'success':'danger'">{{ row.online?'在线':'不可用' }}</ElTag></template></ElTableColumn><ElTableColumn prop="concurrency" label="并发数"/><ElTableColumn prop="runtimes" label="支持环境"/></ElTable><p class="intro">当前版本使用平台内置 Worker，通过 Docker 为每次任务创建独立运行环境。请在部署前构建全部运行时镜像。</p></div></template>
<script setup lang="ts">import {ref,onMounted} from 'vue';import {td} from '@/api/tooldeck';defineOptions({name:'Nodes'});const nodes=ref<any[]>([]);async function load(){nodes.value=await td.list('nodes')};onMounted(load)</script>
<style scoped>.heading{display:flex;justify-content:space-between;margin-bottom:24px}h1{font-size:26px;font-weight:600}.intro{margin-top:20px;color:var(--el-text-color-secondary)}</style>
